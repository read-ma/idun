const initialState = {
    title: 'blank'
};

export default function article(state = initialState, action) {
    return state;
    switch (action.type) {

        default:
            return state;
    }
}
